interface:
  display_name: "BuilderBio"
  short_description: "Generate an evidence-backed BuilderBio recap and publish it"
  default_prompt: "Use $builderbio to scan all of my local coding-agent history, including IDE-hosted logs like Cursor state.vscdb and Gemini-hosted Antigravity conversations, verify source coverage, infer the dominant language from my real sessions, infer whether my AI history is builder, hybrid, or conversation-first, pick a default visual archetype that fits the evidence before asking for override, persist both inferred and chosen mode/theme values, keep module titles and the Make your own CTA in English, avoid meta-copy about the page itself, and apply visual differentiation through layout, typography, density, and chart language instead of accent-only theme swaps before publishing."
